mixin search (keyword)
    form(id="form-search")
        .row  
         .input-group 
            input(
                type="text"
                class="form-control"
                placeholder="Tìm kiếm sản phẩm..." 
                name="keyword" 
                id="search"
                value=keyword)
            .input-group-append
                button(
                class="btn btn-success"
                type="submit"
                ) Tìm kiếm